/* ========================================
   Main Entry Point
   Boots the application
   ======================================== */

document.addEventListener('DOMContentLoaded', () => {
    MobileControls.init();
    Menu.init();
});